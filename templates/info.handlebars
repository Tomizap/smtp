<!DOCTYPE html>
<html>

<head>
    <title>Email Principal</title>
</head>

<body style="background: #80808014;">

    <div style="text-align: center; padding: 20px 0">
        <a href="{{company.links.website}}">
            <img src="{{company.links.logo}}" width="50px" alt="">
        </a>
    </div>
    <table style="table-layout: fixed; width: 100%; padding-top: 30px 0">
        <tr>
            <td style="width: 20%;"></td>
            <td style="border: solid 1px black; border-radius: 10px; background:white; padding: 5%; font-size: 16px">

                <div>{{{html}}}</div>

                {{#if req}}
                <hr style="margin-bottom: 30px;">
                {{> request}}
                {{/if}}

            </td>
            <td style="width: 20%;"></td>
        </tr>
    </table>
    {{> footer}}

</body>

</html>